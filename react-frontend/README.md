# React hello API frontend

Run this app alongside the `../next-backend` project:

```sh
npm install
npm run dev
```

Open http://localhost:5173/test_api. The Vite development server serves the
single-page app at that route via its fallback.
